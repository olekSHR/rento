/**
 * Shared layout tokens for fixed BottomNav coordination.
 * Class definitions live in globals.css (@layer utilities).
 */

/** Content padding when BottomNav is visible (~5.5rem bar + safe area). */
export const BOTTOM_NAV_CONTENT_CLASS = "pb-bottom-nav"

/** Content padding when BottomNav is hidden (safe area only). */
export const SAFE_BOTTOM_CONTENT_CLASS = "pb-safe-bottom"

/** Bottom sheet inner padding above the home indicator. */
export const SHEET_BOTTOM_PADDING_CLASS = "pb-sheet-bottom"
